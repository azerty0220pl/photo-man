import { Chip } from "@mui/material";
import { useState } from "react";

const Tag = ({ name,  }) => {

    return <Chip label={name} variant={state} onClick={() => {
        
    }} />
}

export default Tag;